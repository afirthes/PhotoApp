# Uncomment the next line to define a global platform for your project
platform :ios, '15.2'

target 'PhotoApp' do
  use_frameworks!
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'
  pod 'FirebaseStorage'

  pod 'FirebaseUI'
end
